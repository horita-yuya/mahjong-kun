{
  "name": "mahjong-kun",
  "version": "1.0.0",
  "main": "index.ts",
  "author": "horitayuya@gmail.com",
  "license": "MIT",
  "scripts": {
    "test": "tsc --noEmit",
    "build": "tsc --noEmit && esbuild index.ts --bundle --minify --outfile=dist/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.14.11",
    "typescript": "^4.5.4"
  }
}
